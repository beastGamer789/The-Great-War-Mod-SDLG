name = "!TGW SDLG" # name of your mod, shown in launcher
path = "mod/TGW SDLG" # location of your mod files 
user_dir = "TGW SDLG" # optional. creates a sub directory where settings/map cache/savegames etc can be safely written for your mod and not get mixed up  
